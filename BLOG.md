# MU投屏：把 Android 投屏做得轻一点、快一点、简单一点

有时候，我们只是想把手机画面投到电脑上。

不想安装一整套手机助手，不想登录账号，也不想面对层层菜单；插上数据线，程序认出手机，画面马上出现——这就是 **MU投屏** 想解决的事情。

MU投屏是一款面向 Windows 的迷你 Android 投屏工具，基于开源项目 scrcpy，给常用的投屏流程加上了一个轻巧、直观的中文界面。它把设备识别、画质选择、状态查看和断线重连放到一起，让投屏这件事尽量少打扰你。

> **截图 1｜首屏总览**  
> 建议截图：手机已连接并正在投屏的完整界面，保留右侧手机画面、设备信息卡片和底部日志。  
> 建议文件名：`images/muscrcpy-overview.png`  
> 图片说明：MU投屏主界面——连接状态、设备信息与手机画面一目了然。

## 小，但不是简陋

MU投屏的界面程序使用原生 Win32 C 编写，没有再带一套庞大的网页运行时或 UI 框架。当前 2.3 版的主程序 `MUScrcpy.exe` 约 **144 KB**，启动直接，界面响应也很干脆。

这里的“迷你”主要指 MU投屏自身。为了做到解压后即可使用，完整目录还会附带 adb、scrcpy 及其运行库，现有运行文件合计约 **25 MB**。它不是只有一个孤零零的 EXE，却依然比许多功能繁重的手机管理套件轻得多，而且无需安装、无需账号，也不常驻一堆无关服务。

双击程序后，MU投屏会在后台检查设备。读取手机型号、序列号、Android 版本、当前槽位、内核和系统版本时采用并行处理，不会因为一项信息读取较慢就把整个界面卡住。手机一旦在线，设备信息读取和投屏启动也会同时进行。

这份“轻”不只是包体小，也是使用过程中的克制：打开、连接、投屏，主流程里没有多余步骤。

> **截图 2｜迷你体积**  
> 建议截图：Windows 文件属性窗口，突出 `MUScrcpy.exe` 的大小；旁边可放主界面作为对照。  
> 建议文件名：`images/muscrcpy-size.png`  
> 图片说明：原生编写的 MU投屏主程序仅约 144 KB。

## 插上就投，断了还能自己回来

MU投屏最省心的能力，是**自动回连**。

程序启动后会持续检测 USB 调试设备：

1. 手机尚未连接时，界面显示“等待设备连接”；
2. 设备上线后，自动读取信息并启动投屏；
3. 数据线松动、设备重启，或 scrcpy 意外退出时，程序不会就此罢工；
4. 手机再次在线后，投屏会自动拉起，不需要关闭软件再重开，也不用反复点击“连接”。

这在刷机、调试、演示和长时间观察设备时尤其有用。手机重启是常事，USB 偶尔接触不良也难免。MU投屏把“发现断开—等待设备—重新启动投屏”变成一个自动循环，你只要把线重新接好，剩下的交给它。

> **截图 3｜自动回连过程**  
> 建议截图：将两张状态图并排放置，左侧为“等待设备连接”，右侧为设备恢复后的“正在投屏”；保留下方带时间戳的日志。  
> 建议文件名：`images/muscrcpy-reconnect.png`  
> 图片说明：设备短暂断开后，MU投屏会等待并自动恢复投屏。

## 快，不只是启动快

投屏工具的“快”，最终还是要体现在操作上。

MU投屏默认提供“自动”画质，会根据电脑的处理器、内存以及手机分辨率，在流畅、均衡和高清方案中做选择。你也可以随时手动切换：

| 模式 | 分辨率与帧率 | 码率 | 适合场景 |
| --- | --- | --- | --- |
| 流畅 | 最长边 1280、30 FPS | 4 Mbps | 老电脑、低带宽、优先稳定 |
| 均衡 | 最长边 1920、45 FPS | 8 Mbps | 日常操作与演示 |
| 高清 | 最长边 2560、60 FPS | 12 Mbps | 看重清晰度与流畅度 |
| 极致 | 原始分辨率、最高 120 FPS | 20 Mbps | 高性能设备与高刷体验 |

切换画质后，MU投屏会自动重启 scrcpy 并应用新参数，不需要手动结束旧窗口。设备编码器达不到目标帧率时，则会使用设备实际支持的最高帧率。

投屏窗口启动后会自动靠在控制界面旁边；移动 MU投屏窗口时，程序也会重新校准横向位置，尽量让两个窗口继续相邻。控制区和画面区既各自独立，又能自然地放在一起，整理桌面时很省事。

> **截图 4｜画质选择与窗口贴靠**  
> 建议截图：展开画质下拉菜单，同时展示贴靠在主界面右侧的手机画面。  
> 建议文件名：`images/muscrcpy-quality-dock.png`  
> 图片说明：自动、流畅、均衡、高清、极致五档画质，投屏窗口可自动贴靠。

## 常用功能，都放在顺手的位置

MU投屏没有堆砌功能，但投屏时真正会用到的几项都准备好了：

- **保持亮屏**：投屏期间避免手机自动休眠，并会记住你的选择；
- **投屏置顶**：让手机画面浮在其他窗口之上，适合边看边操作；
- **兼容模式**：遇到显卡渲染或音频兼容问题时，切换到软件渲染并关闭音频；
- **一键点亮屏幕**：检测到手机熄屏后，可直接从电脑端发送亮屏指令；
- **设备信息展示**：快速查看型号、序列号、Android 版本、A/B 槽位、内核和系统版本；
- **实时日志**：连接、启动、重连和参数切换都有记录，出了问题也知道卡在哪一步。

这些选项发生变化时，程序会自动重启投屏进程来应用设置。对使用者来说，就是勾一下或选一下，然后继续工作。

> **截图 5｜熄屏检测与点亮按钮**  
> 建议截图：手机处于熄屏状态，界面状态栏显示“手机屏幕已熄灭”，并出现“点亮屏幕”按钮。  
> 建议文件名：`images/muscrcpy-wake-screen.png`  
> 图片说明：MU投屏可检测熄屏状态，并从电脑端一键点亮手机。

## 三步开始投屏

使用 MU投屏不需要复杂配置：

1. 在 Android 手机的开发者选项中开启 **USB 调试**；
2. 用数据线连接电脑，并在手机上允许这台电脑进行 USB 调试；
3. 解压 MU投屏后运行 `MUScrcpy.exe`，等待程序自动识别并打开投屏画面。

如果普通模式下画面无法正常显示，可以勾选“兼容模式”；电脑性能有限时，可以把画质切换为“流畅”。多数情况下，保持默认的“自动”即可。

## 写在最后

MU投屏没有试图变成一个无所不包的手机管家。它只专注于一件事：**让 Android 手机在 Windows 上更快、更轻、更省心地投出来。**

约 144 KB 的原生控制程序，打开即等待设备；连接后自动投屏，断开后自动回连；再加上自动画质、窗口贴靠、保持亮屏和兼容模式——该有的功能都在，又没有把简单的事情做重。

如果你需要一款随手放进工具箱、插上手机就能工作的迷你投屏工具，MU投屏值得一试。

**项目名称：** MU投屏  
**当前版本：** 2.3  
**作者：** 大侠阿木  
**获取与更新：** [optool.daxiaamu.com/muscrcpy](https://optool.daxiaamu.com/muscrcpy)  
**开源基础：** scrcpy